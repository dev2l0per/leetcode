class Solution:
    def convert(self, s: str, numRows: int) -> str:
        if numRows <= 1:
            return s
        res = ''
        for i in range(numRows):
            
    
sol = Solution()
sol.convert("PAYPALISHIRING", 3)